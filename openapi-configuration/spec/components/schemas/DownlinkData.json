{
  "type": "object",
  "properties": {
    "amount": {
      "type": "number",
      "description": "Downlink data",
      "example": 1
    },
    "unit": {
      "description": "Transmission unit",
      "type": "string",
      "enum": [
        "B",
        "KB",
        "MB"
      ],
      "default": "MB"
    }
  }
}