{
  "description": "Creates new credentials resource for given connection_id.",
  "required": true,
  "content": {
    "application/json": {
      "schema": {
        "$ref": "../schemas/CustomStorageConfiguration.json"
      }
    }
  }
}