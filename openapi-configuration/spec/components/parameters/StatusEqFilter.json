{
  "name": "filter[status][eq]",
  "in": "query",
  "description": "Filters records to those with a specific status.",
  "schema": {
    "type": "string",
    "enum": [
      "pending",
      "completed",
      "failed"
    ],
    "example": "pending"
  }
}