{
  "name": "filter[status][in][]",
  "in": "query",
  "description": "Filters records to those with a least one status in the list.",
  "schema": {
    "type": "array",
    "items": {
      "type": "string",
      "enum": [
        "pending",
        "completed",
        "failed"
      ],
      "example": "pending"
    }
  }
}