{
  "name": "filter[product]",
  "in": "query",
  "description": "Filter by product.",
  "required": false,
  "schema": {
    "type": "string",
    "enum": [
      "call_control",
      "fax",
      "texml"
    ],
    "example": "texml"
  }
}