{
  "name": "filter[status][eq]",
  "in": "query",
  "description": "The status of the release to filter by",
  "required": false,
  "schema": {
    "type": "array",
    "items": {
      "type": "string",
      "enum": [
        "pending_upload",
        "pending",
        "in_progress",
        "complete",
        "failed",
        "expired",
        "unknown"
      ]
    },
    "example": [
      "pending",
      "in_progress"
    ]
  }
}