{
  "name": "type",
  "in": "query",
  "required": false,
  "description": "Specifies the type of number lookup to be performed",
  "schema": {
    "type": "string",
    "enum": [
      "carrier",
      "caller-name"
    ]
  }
}