{
  "name": "Status",
  "in": "query",
  "required": false,
  "description": "Filters calls by status.",
  "schema": {
    "type": "string",
    "enum": [
      "canceled",
      "completed",
      "failed",
      "busy",
      "no-answer"
    ]
  },
  "example": "no-answer"
}