{
  "name": "filter[channel_type_id][eq]",
  "in": "query",
  "description": "Filter by the id of a channel type",
  "schema": {
    "type": "string",
    "enum": [
      "webhook",
      "sms",
      "email",
      "voice"
    ],
    "example": "webhook"
  }
}