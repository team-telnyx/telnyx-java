{
  "type": "array",
  "description": "A list of mobile network operators and the priority that should be applied when the SIM is connecting to the network.",
  "items": {
    "$ref": "./MobileNetworkOperatorPreferencesResponse.json"
  }
}