{
  "properties": {
    "bucket_ids": {
      "items": {
        "type": "string"
      },
      "type": "array",
      "title": "Bucket Ids"
    },
    "max_num_results": {
      "description": "The maximum number of results to retrieve as context for the language model.",
      "type": "integer"
    }
  },
  "type": "object",
  "required": [
    "bucket_ids"
  ],
  "title": "BucketIds"
}