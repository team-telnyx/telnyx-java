{
  "type": "object",
  "properties": {
    "amount": {
      "type": "string",
      "description": "Rate from which cost is calculated",
      "example": 0.1
    },
    "currency": {
      "type": "string",
      "description": "Currency of the rate and cost",
      "example": "USD",
      "enum": [
        "AUD",
        "CAD",
        "EUR",
        "GBP",
        "USD"
      ]
    }
  }
}