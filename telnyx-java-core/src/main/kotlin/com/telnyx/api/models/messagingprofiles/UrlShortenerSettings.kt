// File generated from our OpenAPI spec by Stainless.

package com.telnyx.api.models.messagingprofiles

import com.fasterxml.jackson.annotation.JsonAnyGetter
import com.fasterxml.jackson.annotation.JsonAnySetter
import com.fasterxml.jackson.annotation.JsonCreator
import com.fasterxml.jackson.annotation.JsonProperty
import com.telnyx.api.core.ExcludeMissing
import com.telnyx.api.core.JsonField
import com.telnyx.api.core.JsonMissing
import com.telnyx.api.core.JsonValue
import com.telnyx.api.core.checkRequired
import com.telnyx.api.errors.TelnyxInvalidDataException
import java.util.Collections
import java.util.Objects
import java.util.Optional

/**
 * The URL shortener feature allows automatic replacement of URLs that were generated using a public
 * URL shortener service. Some examples include bit.do, bit.ly, goo.gl, ht.ly, is.gd, ow.ly,
 * rebrand.ly, t.co, tiny.cc, and tinyurl.com. Such URLs are replaced with with links generated by
 * Telnyx. The use of custom links can improve branding and message deliverability.
 *
 * To disable this feature, set the object field to `null`.
 */
class UrlShortenerSettings
@JsonCreator(mode = JsonCreator.Mode.DISABLED)
private constructor(
    private val domain: JsonField<String>,
    private val prefix: JsonField<String>,
    private val replaceBlacklistOnly: JsonField<Boolean>,
    private val sendWebhooks: JsonField<Boolean>,
    private val additionalProperties: MutableMap<String, JsonValue>,
) {

    @JsonCreator
    private constructor(
        @JsonProperty("domain") @ExcludeMissing domain: JsonField<String> = JsonMissing.of(),
        @JsonProperty("prefix") @ExcludeMissing prefix: JsonField<String> = JsonMissing.of(),
        @JsonProperty("replace_blacklist_only")
        @ExcludeMissing
        replaceBlacklistOnly: JsonField<Boolean> = JsonMissing.of(),
        @JsonProperty("send_webhooks")
        @ExcludeMissing
        sendWebhooks: JsonField<Boolean> = JsonMissing.of(),
    ) : this(domain, prefix, replaceBlacklistOnly, sendWebhooks, mutableMapOf())

    /**
     * One of the domains provided by the Telnyx URL shortener service.
     *
     * @throws TelnyxInvalidDataException if the JSON field has an unexpected type or is
     *   unexpectedly missing or null (e.g. if the server responded with an unexpected value).
     */
    fun domain(): String = domain.getRequired("domain")

    /**
     * Optional prefix that can be used to identify your brand, and will appear in the Telnyx
     * generated URLs after the domain name.
     *
     * @throws TelnyxInvalidDataException if the JSON field has an unexpected type (e.g. if the
     *   server responded with an unexpected value).
     */
    fun prefix(): Optional<String> = prefix.getOptional("prefix")

    /**
     * Use the link replacement tool only for links that are specifically blacklisted by Telnyx.
     *
     * @throws TelnyxInvalidDataException if the JSON field has an unexpected type (e.g. if the
     *   server responded with an unexpected value).
     */
    fun replaceBlacklistOnly(): Optional<Boolean> =
        replaceBlacklistOnly.getOptional("replace_blacklist_only")

    /**
     * Receive webhooks for when your replaced links are clicked. Webhooks are sent to the webhooks
     * on the messaging profile.
     *
     * @throws TelnyxInvalidDataException if the JSON field has an unexpected type (e.g. if the
     *   server responded with an unexpected value).
     */
    fun sendWebhooks(): Optional<Boolean> = sendWebhooks.getOptional("send_webhooks")

    /**
     * Returns the raw JSON value of [domain].
     *
     * Unlike [domain], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("domain") @ExcludeMissing fun _domain(): JsonField<String> = domain

    /**
     * Returns the raw JSON value of [prefix].
     *
     * Unlike [prefix], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("prefix") @ExcludeMissing fun _prefix(): JsonField<String> = prefix

    /**
     * Returns the raw JSON value of [replaceBlacklistOnly].
     *
     * Unlike [replaceBlacklistOnly], this method doesn't throw if the JSON field has an unexpected
     * type.
     */
    @JsonProperty("replace_blacklist_only")
    @ExcludeMissing
    fun _replaceBlacklistOnly(): JsonField<Boolean> = replaceBlacklistOnly

    /**
     * Returns the raw JSON value of [sendWebhooks].
     *
     * Unlike [sendWebhooks], this method doesn't throw if the JSON field has an unexpected type.
     */
    @JsonProperty("send_webhooks")
    @ExcludeMissing
    fun _sendWebhooks(): JsonField<Boolean> = sendWebhooks

    @JsonAnySetter
    private fun putAdditionalProperty(key: String, value: JsonValue) {
        additionalProperties.put(key, value)
    }

    @JsonAnyGetter
    @ExcludeMissing
    fun _additionalProperties(): Map<String, JsonValue> =
        Collections.unmodifiableMap(additionalProperties)

    fun toBuilder() = Builder().from(this)

    companion object {

        /**
         * Returns a mutable builder for constructing an instance of [UrlShortenerSettings].
         *
         * The following fields are required:
         * ```java
         * .domain()
         * ```
         */
        @JvmStatic fun builder() = Builder()
    }

    /** A builder for [UrlShortenerSettings]. */
    class Builder internal constructor() {

        private var domain: JsonField<String>? = null
        private var prefix: JsonField<String> = JsonMissing.of()
        private var replaceBlacklistOnly: JsonField<Boolean> = JsonMissing.of()
        private var sendWebhooks: JsonField<Boolean> = JsonMissing.of()
        private var additionalProperties: MutableMap<String, JsonValue> = mutableMapOf()

        @JvmSynthetic
        internal fun from(urlShortenerSettings: UrlShortenerSettings) = apply {
            domain = urlShortenerSettings.domain
            prefix = urlShortenerSettings.prefix
            replaceBlacklistOnly = urlShortenerSettings.replaceBlacklistOnly
            sendWebhooks = urlShortenerSettings.sendWebhooks
            additionalProperties = urlShortenerSettings.additionalProperties.toMutableMap()
        }

        /** One of the domains provided by the Telnyx URL shortener service. */
        fun domain(domain: String) = domain(JsonField.of(domain))

        /**
         * Sets [Builder.domain] to an arbitrary JSON value.
         *
         * You should usually call [Builder.domain] with a well-typed [String] value instead. This
         * method is primarily for setting the field to an undocumented or not yet supported value.
         */
        fun domain(domain: JsonField<String>) = apply { this.domain = domain }

        /**
         * Optional prefix that can be used to identify your brand, and will appear in the Telnyx
         * generated URLs after the domain name.
         */
        fun prefix(prefix: String) = prefix(JsonField.of(prefix))

        /**
         * Sets [Builder.prefix] to an arbitrary JSON value.
         *
         * You should usually call [Builder.prefix] with a well-typed [String] value instead. This
         * method is primarily for setting the field to an undocumented or not yet supported value.
         */
        fun prefix(prefix: JsonField<String>) = apply { this.prefix = prefix }

        /**
         * Use the link replacement tool only for links that are specifically blacklisted by Telnyx.
         */
        fun replaceBlacklistOnly(replaceBlacklistOnly: Boolean) =
            replaceBlacklistOnly(JsonField.of(replaceBlacklistOnly))

        /**
         * Sets [Builder.replaceBlacklistOnly] to an arbitrary JSON value.
         *
         * You should usually call [Builder.replaceBlacklistOnly] with a well-typed [Boolean] value
         * instead. This method is primarily for setting the field to an undocumented or not yet
         * supported value.
         */
        fun replaceBlacklistOnly(replaceBlacklistOnly: JsonField<Boolean>) = apply {
            this.replaceBlacklistOnly = replaceBlacklistOnly
        }

        /**
         * Receive webhooks for when your replaced links are clicked. Webhooks are sent to the
         * webhooks on the messaging profile.
         */
        fun sendWebhooks(sendWebhooks: Boolean) = sendWebhooks(JsonField.of(sendWebhooks))

        /**
         * Sets [Builder.sendWebhooks] to an arbitrary JSON value.
         *
         * You should usually call [Builder.sendWebhooks] with a well-typed [Boolean] value instead.
         * This method is primarily for setting the field to an undocumented or not yet supported
         * value.
         */
        fun sendWebhooks(sendWebhooks: JsonField<Boolean>) = apply {
            this.sendWebhooks = sendWebhooks
        }

        fun additionalProperties(additionalProperties: Map<String, JsonValue>) = apply {
            this.additionalProperties.clear()
            putAllAdditionalProperties(additionalProperties)
        }

        fun putAdditionalProperty(key: String, value: JsonValue) = apply {
            additionalProperties.put(key, value)
        }

        fun putAllAdditionalProperties(additionalProperties: Map<String, JsonValue>) = apply {
            this.additionalProperties.putAll(additionalProperties)
        }

        fun removeAdditionalProperty(key: String) = apply { additionalProperties.remove(key) }

        fun removeAllAdditionalProperties(keys: Set<String>) = apply {
            keys.forEach(::removeAdditionalProperty)
        }

        /**
         * Returns an immutable instance of [UrlShortenerSettings].
         *
         * Further updates to this [Builder] will not mutate the returned instance.
         *
         * The following fields are required:
         * ```java
         * .domain()
         * ```
         *
         * @throws IllegalStateException if any required field is unset.
         */
        fun build(): UrlShortenerSettings =
            UrlShortenerSettings(
                checkRequired("domain", domain),
                prefix,
                replaceBlacklistOnly,
                sendWebhooks,
                additionalProperties.toMutableMap(),
            )
    }

    private var validated: Boolean = false

    fun validate(): UrlShortenerSettings = apply {
        if (validated) {
            return@apply
        }

        domain()
        prefix()
        replaceBlacklistOnly()
        sendWebhooks()
        validated = true
    }

    fun isValid(): Boolean =
        try {
            validate()
            true
        } catch (e: TelnyxInvalidDataException) {
            false
        }

    /**
     * Returns a score indicating how many valid values are contained in this object recursively.
     *
     * Used for best match union deserialization.
     */
    @JvmSynthetic
    internal fun validity(): Int =
        (if (domain.asKnown().isPresent) 1 else 0) +
            (if (prefix.asKnown().isPresent) 1 else 0) +
            (if (replaceBlacklistOnly.asKnown().isPresent) 1 else 0) +
            (if (sendWebhooks.asKnown().isPresent) 1 else 0)

    override fun equals(other: Any?): Boolean {
        if (this === other) {
            return true
        }

        return other is UrlShortenerSettings &&
            domain == other.domain &&
            prefix == other.prefix &&
            replaceBlacklistOnly == other.replaceBlacklistOnly &&
            sendWebhooks == other.sendWebhooks &&
            additionalProperties == other.additionalProperties
    }

    private val hashCode: Int by lazy {
        Objects.hash(domain, prefix, replaceBlacklistOnly, sendWebhooks, additionalProperties)
    }

    override fun hashCode(): Int = hashCode

    override fun toString() =
        "UrlShortenerSettings{domain=$domain, prefix=$prefix, replaceBlacklistOnly=$replaceBlacklistOnly, sendWebhooks=$sendWebhooks, additionalProperties=$additionalProperties}"
}
