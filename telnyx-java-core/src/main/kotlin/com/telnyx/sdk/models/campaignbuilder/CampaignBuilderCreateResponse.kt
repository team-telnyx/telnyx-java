// File generated from our OpenAPI spec by Stainless.

package com.telnyx.sdk.models.campaignbuilder

import com.fasterxml.jackson.core.JsonGenerator
import com.fasterxml.jackson.core.ObjectCodec
import com.fasterxml.jackson.databind.JsonNode
import com.fasterxml.jackson.databind.SerializerProvider
import com.fasterxml.jackson.databind.annotation.JsonDeserialize
import com.fasterxml.jackson.databind.annotation.JsonSerialize
import com.fasterxml.jackson.module.kotlin.jacksonTypeRef
import com.telnyx.sdk.core.BaseDeserializer
import com.telnyx.sdk.core.BaseSerializer
import com.telnyx.sdk.core.JsonValue
import com.telnyx.sdk.core.allMaxBy
import com.telnyx.sdk.core.getOrThrow
import com.telnyx.sdk.errors.TelnyxInvalidDataException
import com.telnyx.sdk.models.campaign.TelnyxCampaignCsp
import java.util.Objects
import java.util.Optional

/**
 * Campaign is generated by the 10DLC registry once the corresponding campaign request is approved.
 * Each campaign is assigned a unique identifier - **campaignId**. Once a campaign is activated,
 * limited information is published to the NetNumber OSR service for consumption by members of the
 * ecosystem. When a campaign is suspended(reversible) or expired(non-reversible), campaign data is
 * deleted from the OSR service. Most attributes of campaignare immutable, including **usecase**,
 * **vertical**, **brandId** and **cspId**.
 */
@JsonDeserialize(using = CampaignBuilderCreateResponse.Deserializer::class)
@JsonSerialize(using = CampaignBuilderCreateResponse.Serializer::class)
class CampaignBuilderCreateResponse
private constructor(
    private val jsonValue: JsonValue? = null,
    private val telnyxCampaignCsp: TelnyxCampaignCsp? = null,
    private val _json: JsonValue? = null,
) {

    fun jsonValue(): Optional<JsonValue> = Optional.ofNullable(jsonValue)

    /**
     * Campaign is generated by the 10DLC registry once the corresponding campaign request is
     * approved. Each campaign is assigned a unique identifier - **campaignId**. Once a campaign is
     * activated, limited information is published to the NetNumber OSR service for consumption by
     * members of the ecosystem. When a campaign is suspended(reversible) or
     * expired(non-reversible), campaign data is deleted from the OSR service. Most attributes of
     * campaignare immutable, including **usecase**, **vertical**, **brandId** and **cspId**.
     */
    fun telnyxCampaignCsp(): Optional<TelnyxCampaignCsp> = Optional.ofNullable(telnyxCampaignCsp)

    fun isJsonValue(): Boolean = jsonValue != null

    fun isTelnyxCampaignCsp(): Boolean = telnyxCampaignCsp != null

    fun asJsonValue(): JsonValue = jsonValue.getOrThrow("jsonValue")

    /**
     * Campaign is generated by the 10DLC registry once the corresponding campaign request is
     * approved. Each campaign is assigned a unique identifier - **campaignId**. Once a campaign is
     * activated, limited information is published to the NetNumber OSR service for consumption by
     * members of the ecosystem. When a campaign is suspended(reversible) or
     * expired(non-reversible), campaign data is deleted from the OSR service. Most attributes of
     * campaignare immutable, including **usecase**, **vertical**, **brandId** and **cspId**.
     */
    fun asTelnyxCampaignCsp(): TelnyxCampaignCsp = telnyxCampaignCsp.getOrThrow("telnyxCampaignCsp")

    fun _json(): Optional<JsonValue> = Optional.ofNullable(_json)

    fun <T> accept(visitor: Visitor<T>): T =
        when {
            jsonValue != null -> visitor.visitJsonValue(jsonValue)
            telnyxCampaignCsp != null -> visitor.visitTelnyxCampaignCsp(telnyxCampaignCsp)
            else -> visitor.unknown(_json)
        }

    private var validated: Boolean = false

    fun validate(): CampaignBuilderCreateResponse = apply {
        if (validated) {
            return@apply
        }

        accept(
            object : Visitor<Unit> {
                override fun visitJsonValue(jsonValue: JsonValue) {}

                override fun visitTelnyxCampaignCsp(telnyxCampaignCsp: TelnyxCampaignCsp) {
                    telnyxCampaignCsp.validate()
                }
            }
        )
        validated = true
    }

    fun isValid(): Boolean =
        try {
            validate()
            true
        } catch (e: TelnyxInvalidDataException) {
            false
        }

    /**
     * Returns a score indicating how many valid values are contained in this object recursively.
     *
     * Used for best match union deserialization.
     */
    @JvmSynthetic
    internal fun validity(): Int =
        accept(
            object : Visitor<Int> {
                override fun visitJsonValue(jsonValue: JsonValue) = 1

                override fun visitTelnyxCampaignCsp(telnyxCampaignCsp: TelnyxCampaignCsp) =
                    telnyxCampaignCsp.validity()

                override fun unknown(json: JsonValue?) = 0
            }
        )

    override fun equals(other: Any?): Boolean {
        if (this === other) {
            return true
        }

        return other is CampaignBuilderCreateResponse &&
            jsonValue == other.jsonValue &&
            telnyxCampaignCsp == other.telnyxCampaignCsp
    }

    override fun hashCode(): Int = Objects.hash(jsonValue, telnyxCampaignCsp)

    override fun toString(): String =
        when {
            jsonValue != null -> "CampaignBuilderCreateResponse{jsonValue=$jsonValue}"
            telnyxCampaignCsp != null ->
                "CampaignBuilderCreateResponse{telnyxCampaignCsp=$telnyxCampaignCsp}"
            _json != null -> "CampaignBuilderCreateResponse{_unknown=$_json}"
            else -> throw IllegalStateException("Invalid CampaignBuilderCreateResponse")
        }

    companion object {

        @JvmStatic
        fun ofJsonValue(jsonValue: JsonValue) = CampaignBuilderCreateResponse(jsonValue = jsonValue)

        /**
         * Campaign is generated by the 10DLC registry once the corresponding campaign request is
         * approved. Each campaign is assigned a unique identifier - **campaignId**. Once a campaign
         * is activated, limited information is published to the NetNumber OSR service for
         * consumption by members of the ecosystem. When a campaign is suspended(reversible) or
         * expired(non-reversible), campaign data is deleted from the OSR service. Most attributes
         * of campaignare immutable, including **usecase**, **vertical**, **brandId** and **cspId**.
         */
        @JvmStatic
        fun ofTelnyxCampaignCsp(telnyxCampaignCsp: TelnyxCampaignCsp) =
            CampaignBuilderCreateResponse(telnyxCampaignCsp = telnyxCampaignCsp)
    }

    /**
     * An interface that defines how to map each variant of [CampaignBuilderCreateResponse] to a
     * value of type [T].
     */
    interface Visitor<out T> {

        fun visitJsonValue(jsonValue: JsonValue): T

        /**
         * Campaign is generated by the 10DLC registry once the corresponding campaign request is
         * approved. Each campaign is assigned a unique identifier - **campaignId**. Once a campaign
         * is activated, limited information is published to the NetNumber OSR service for
         * consumption by members of the ecosystem. When a campaign is suspended(reversible) or
         * expired(non-reversible), campaign data is deleted from the OSR service. Most attributes
         * of campaignare immutable, including **usecase**, **vertical**, **brandId** and **cspId**.
         */
        fun visitTelnyxCampaignCsp(telnyxCampaignCsp: TelnyxCampaignCsp): T

        /**
         * Maps an unknown variant of [CampaignBuilderCreateResponse] to a value of type [T].
         *
         * An instance of [CampaignBuilderCreateResponse] can contain an unknown variant if it was
         * deserialized from data that doesn't match any known variant. For example, if the SDK is
         * on an older version than the API, then the API may respond with new variants that the SDK
         * is unaware of.
         *
         * @throws TelnyxInvalidDataException in the default implementation.
         */
        fun unknown(json: JsonValue?): T {
            throw TelnyxInvalidDataException("Unknown CampaignBuilderCreateResponse: $json")
        }
    }

    internal class Deserializer :
        BaseDeserializer<CampaignBuilderCreateResponse>(CampaignBuilderCreateResponse::class) {

        override fun ObjectCodec.deserialize(node: JsonNode): CampaignBuilderCreateResponse {
            val json = JsonValue.fromJsonNode(node)

            val bestMatches =
                sequenceOf(
                        tryDeserialize(node, jacksonTypeRef<TelnyxCampaignCsp>())?.let {
                            CampaignBuilderCreateResponse(telnyxCampaignCsp = it, _json = json)
                        },
                        tryDeserialize(node, jacksonTypeRef<JsonValue>())?.let {
                            CampaignBuilderCreateResponse(jsonValue = it, _json = json)
                        },
                    )
                    .filterNotNull()
                    .allMaxBy { it.validity() }
                    .toList()
            return when (bestMatches.size) {
                // This can happen if what we're deserializing is completely incompatible with all
                // the possible variants.
                0 -> CampaignBuilderCreateResponse(_json = json)
                1 -> bestMatches.single()
                // If there's more than one match with the highest validity, then use the first
                // completely valid match, or simply the first match if none are completely valid.
                else -> bestMatches.firstOrNull { it.isValid() } ?: bestMatches.first()
            }
        }
    }

    internal class Serializer :
        BaseSerializer<CampaignBuilderCreateResponse>(CampaignBuilderCreateResponse::class) {

        override fun serialize(
            value: CampaignBuilderCreateResponse,
            generator: JsonGenerator,
            provider: SerializerProvider,
        ) {
            when {
                value.jsonValue != null -> generator.writeObject(value.jsonValue)
                value.telnyxCampaignCsp != null -> generator.writeObject(value.telnyxCampaignCsp)
                value._json != null -> generator.writeObject(value._json)
                else -> throw IllegalStateException("Invalid CampaignBuilderCreateResponse")
            }
        }
    }
}
